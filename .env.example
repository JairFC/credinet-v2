# =============================================================================
# CREDINET v2.0 - Environment Variables Template
# =============================================================================
# Copia este archivo como .env y ajusta los valores para tu entorno
# NUNCA subas el archivo .env a Git
# =============================================================================

# =============================================================================
# POSTGRES DATABASE
# =============================================================================
POSTGRES_USER=credinet_user
POSTGRES_PASSWORD=credinet_pass_change_this_in_production
POSTGRES_DB=credinet_db
POSTGRES_PORT=5432

# =============================================================================
# BACKEND API
# =============================================================================
BACKEND_PORT=8000

# Application Environment
ENVIRONMENT=development                                    # development | staging | production
DEBUG=true                                                 # false en producción
LOG_LEVEL=INFO                                            # DEBUG | INFO | WARNING | ERROR

# JWT Security
SECRET_KEY=your_secret_key_min_32_chars_change_this       # CAMBIAR en producción (min 32 chars)
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Origins (separados por comas, sin espacios)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# File Upload
MAX_UPLOAD_SIZE=10485760                                  # 10MB en bytes

# =============================================================================
# FRONTEND
# =============================================================================
FRONTEND_PORT=5173

# Vite Environment Variables (deben empezar con VITE_)
VITE_API_URL=http://localhost:8000
VITE_APP_NAME=Credinet
VITE_APP_VERSION=2.0.0

# Hot Reload (Development only)
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true

# =============================================================================
# DOCKER
# =============================================================================
DOCKER_TARGET=development                                 # development | production

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# Para producción, asegúrate de cambiar:
# 1. POSTGRES_PASSWORD - usar contraseña segura (min 16 chars)
# 2. SECRET_KEY - generar con: openssl rand -hex 32
# 3. DEBUG=false
# 4. ENVIRONMENT=production
# 5. CORS_ORIGINS - solo dominios autorizados
# 6. Usar HTTPS y certificados SSL/TLS
# =============================================================================
